# REST-API-with-Flask

# Features
Create user → POST,
Get all users → GET,
Get single user → GET by ID,
Update user → PUT,
Delete user → DELETE,
Works with Postman / Curl.

# Project Structure
flask_user_api/
 ├── app.py
 └── requirements.txt
